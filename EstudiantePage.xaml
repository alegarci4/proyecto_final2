<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="edu_comp.EstudiantePage"
             Title="FormEstudiante">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20">

            <Label x:Name="lblBienvenida" FontSize="20" HorizontalOptions="Center" />

            <Entry x:Name="txtConsulta" Placeholder="Escribe tu consulta aquí..." />

            <Button Text="Buscar" Clicked="BtnBuscar_Click" />

            <Editor x:Name="txtResultado" HeightRequest="200" IsReadOnly="True" />

            <HorizontalStackLayout Spacing="20" HorizontalOptions="Center">
                <Button Text="Ver mi historial" Clicked="BtnHistorial_Click" />
                <Button Text="Cerrar" Clicked="BtnCerrar_Click" />
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
